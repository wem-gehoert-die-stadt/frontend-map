/* eslint-disable import/prefer-default-export */
import styled from 'styled-components'

import { TableCell } from '@material-ui/core'

export const YearColumn = styled(TableCell)`
  width: 8.3em;
  text-align: right;
`
